spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: 1234
        url: jdbc:postgresql://localhost:5444/fornecedor
        username: postgres
    jpa:
        hibernate:
